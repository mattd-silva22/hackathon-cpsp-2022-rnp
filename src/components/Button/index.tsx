import React from "react";

import styled from "styled-components";

const Button: React.FC = ({ prefix, text, onClick }) => {
  return <Container />;
};

export default Button;

export const Container = styled.div``;
